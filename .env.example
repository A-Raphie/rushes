# Solari: create at console.getsolari.com (API Keys tab, shown once)
SOLARI_API_KEY=slr_live_replace_me

# Groq: free tier, console.groq.com/keys
GROQ_API_KEY=gsk_replace_me

# Optional: where Rushes writes runs (defaults to ./runs)
RUSHES_OUT=./runs
