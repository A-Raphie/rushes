# X post draft

Status: DRAFT · needs his voice pass + the demo video link added when it exists
Tags: @harrychow_ @getsolari · link: https://tryrushes.vercel.app

---

Draft A (plain, receipts-led):

For the @getsolari challenge we built Rushes: a proof layer for agent runs.

Point it at a task. A recorded Solari cloud browser executes it, and the
receipt commits itself to a public repo: the tape, the manifest, a verdict,
a serial.

No screen recordings you have to trust. The replay lives on Solari's own
servers, and the tape is committed byte for byte.

7 runs on the record, built on the free tier. Submit your own:
https://tryrushes.vercel.app/point

@harrychow_ @getsolari

---

Draft B (danger-flip lead):

Agent demos have a trust problem: a screenshot and a claim prove nothing.

So for the @getsolari challenge we built Rushes. Point it at a task, a
recorded Solari cloud browser runs it, and you get a public receipt: the
tape, the manifest, a verdict, a serial. The replay is hosted on Solari's
own servers, not your drive.

Runs from Sep 1 still play. Runs from Sep 2 are marked failed because
Solari's replay generation went down. A receipts product that hides that
would not be a receipts product.

https://tryrushes.vercel.app

@harrychow_ @getsolari

---

Notes for his voice pass:
- Pick one draft, or mix: B's opening + A's submit line
- The demo video link slots after the first line when it exists
- Both drafts pass the lint: no em dashes, no puff, numbers only where real
